# ─────────────────────────────────────────────────────────────────────────────
# AIRP — backend/requirements-dev.txt
# Development-only dependencies. NOT installed in production.
# Install with: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Pre-commit framework ───────────────────────────────────────────────────
pre-commit==3.7.0

# ── Formatting ────────────────────────────────────────────────────────────
black==24.3.0
isort==5.13.2

# ── Linting ───────────────────────────────────────────────────────────────
flake8==7.0.0
flake8-bugbear==24.2.6
flake8-comprehensions==3.14.0
flake8-docstrings==1.7.0

# ── Type checking ─────────────────────────────────────────────────────────
mypy==1.9.0
pydantic[mypy]>=2.7.4,<3.0.0   # ships pydantic.mypy plugin

# ── Settings (needed to import config.py in tests) ────────────────────────
pydantic-settings==2.2.1        # loads .env into Pydantic Settings models

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0
pytest-mock==3.14.0             # mocker fixture (MagicMock helpers)
httpx==0.27.0                   # async test client for FastAPI

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1            # load .env in test sessions

# ── LLM & Agent Orchestration ─────────────────────────────────────────────
langchain-groq>=0.2.0,<0.3.0
anthropic>=0.27.0,<1.0.0        # Claude API client — backbone for all 8 agents
langchain==0.3.0                # tools, chains, and prompt templates
langgraph==0.2.28               # multi-agent state machine orchestration
langsmith>=0.1.17,<0.2.0        # tracing, evaluation, and debugging

# ── API & Backend ─────────────────────────────────────────────────────────
fastapi==0.110.0                # REST + WebSocket framework
uvicorn==0.29.0                 # ASGI server for FastAPI
python-multipart==0.0.9         # required for FastAPI file uploads (PDF reports)
python-jose[cryptography]==3.3.0 # JWT token handling in FastAPI

# ── Database ──────────────────────────────────────────────────────────────
asyncpg==0.29.0                 # async PostgreSQL driver (required for SQLAlchemy async)
sqlalchemy==2.0.0               # ORM — industry standard Python database toolkit
alembic==1.13.0                 # database migrations — version-controlled schema changes
greenlet==3.0.3                 # required by SQLAlchemy async internally

# ── Cache ─────────────────────────────────────────────────────────────────
redis==5.0.0                    # Redis client — API response caching via Upstash

# ── Market Data ───────────────────────────────────────────────────────────
yfinance==0.2.40                # stock prices, OHLCV, financials — no API key required
requests==2.31.0                # HTTP client for NewsAPI, Alpha Vantage, Screener scraping
beautifulsoup4==4.12.3          # HTML parsing for Screener.in and RBI scraping
lxml==5.2.1                     # faster HTML parser used by BeautifulSoup

# ── Vector Store & Embeddings ─────────────────────────────────────────────
chromadb==0.5.0                 # vector database for RAG — news and earnings embeddings
sentence-transformers==3.0.0    # local text embeddings — no API cost, runs on device

# ── PDF Generation ────────────────────────────────────────────────────────
weasyprint==62.0                # generates the final Investment Memo PDF

# ── Utilities ─────────────────────────────────────────────────────────────
tenacity==8.2.3                 # retry logic for flaky API calls (yFinance, NewsAPI)
aiohttp==3.9.5                  # async HTTP — needed by some LangChain tools internally
