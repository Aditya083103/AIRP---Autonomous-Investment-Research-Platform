# =============================================================================
# AIRP — backend/requirements-dev.txt
# Development-only dependencies. NOT installed in production (Render).
#
# Install order:
#   pip install -r requirements.txt      ← production deps first
#   pip install -r requirements-dev.txt  ← then dev deps on top
#
# These packages are never imported by application code — only by tests,
# linters, type checkers, and git hooks.
# =============================================================================


# ── Pre-commit & Git Hooks ────────────────────────────────────────────────────
pre-commit==3.7.0


# ── Code Formatting ───────────────────────────────────────────────────────────
black==24.4.2
isort==5.13.2


# ── Linting ───────────────────────────────────────────────────────────────────
flake8==7.0.0
flake8-bugbear==24.4.26          # Catches common bugs and design issues
flake8-comprehensions==3.14.0    # Enforces cleaner list/dict comprehensions
flake8-docstrings==1.7.0         # Enforces docstrings on public functions


# ── Type Checking ─────────────────────────────────────────────────────────────
mypy==1.10.0
pydantic[mypy]==2.7.4            # Ships pydantic.mypy plugin for strict checks


# ── Testing Framework ─────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7           # async test functions with asyncio_mode = auto
pytest-cov==5.0.0                # Coverage reporting; enforces 85% threshold
pytest-mock==3.14.0              # mocker fixture — MagicMock / AsyncMock helpers
httpx==0.27.0                    # Async test client for FastAPI (TestClient)


# ── Test Utilities ────────────────────────────────────────────────────────────
freezegun==1.5.1                 # Freeze time in tests that call datetime.now()
respx==0.21.1                    # Mock httpx requests in async tests
